---
import Button from '../ui/Button.astro';

interface Props {
  title: string;
  subtitle?: string;
  backgroundImage?: string;
  ctaText?: string;
  ctaLink?: string;
  height?: 'small' | 'medium' | 'large';
}

const { title, subtitle, backgroundImage, ctaText, ctaLink, height = 'large' } = Astro.props;

const heightClasses = {
  small: 'h-64 md:h-80',
  medium: 'h-80 md:h-96',
  large: 'h-96 md:h-[32rem]',
};
---

<section
  class:list={[
    'relative flex items-center justify-center',
    heightClasses[height],
    backgroundImage && 'bg-cover bg-center',
  ]}
  style={backgroundImage ? `background-image: url(${backgroundImage})` : undefined}
>
  <div class="absolute inset-0 bg-gradient-to-b from-black/60 to-black/40"></div>

  <div class="container relative z-10 mx-auto px-4 text-center text-white">
    <h1 class="text-4xl font-bold md:text-5xl lg:text-6xl">
      {title}
    </h1>
    {subtitle && <p class="mt-4 text-lg md:text-xl lg:text-2xl font-medium">{subtitle}</p>}
    {
      ctaText && ctaLink && (
        <div class="mt-8">
          <Button href={ctaLink} variant="primary" size="large">
            {ctaText}
          </Button>
        </div>
      )
    }
  </div>
</section>
